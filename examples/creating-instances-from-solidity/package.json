{
  "name": "example-creating-instances-from-solidity",
  "version": "2.4.0-rc.0",
  "private": true,
  "description": "",
  "main": "truffle-config.js",
  "scripts": {
    "compile": "rm -rf build && truffle compile",
    "start": "npm run compile && truffle exec index.js --network local",
    "test": "npm run compile && truffle test --network test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "zos-lib": "^2.4.0-rc.0"
  },
  "devDependencies": {
    "truffle": "^5.0.5",
    "zos": "^2.4.0-rc.0"
  }
}
